import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {flex: 1, padding: 16},
  status: {marginBottom: 10, flexDirection: 'row', alignItems: 'center'},
  switch: {marginLeft: 10},
});

export default styles;
